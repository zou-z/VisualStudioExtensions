<UserControl
    x:Class="ResxResourceExtension.View.TextNotificationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ResxResourceExtension.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="clr-namespace:ResxResourceExtension.ViewModel"
    d:DataContext="{d:DesignInstance Type=viewmodel:TextNotificationViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <TextBlock
        Foreground="{Binding ColorBrush}"
        Text="{Binding Text}"
        Visibility="{Binding Visibility}" />
</UserControl>
